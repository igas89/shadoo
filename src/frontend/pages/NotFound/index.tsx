import React, { FC } from 'react';
import './NotFound.scss';

const NotFound: FC = () => (
        <div className='notFound'>
            Страница не найдена :(
        </div>
    );

export default NotFound;